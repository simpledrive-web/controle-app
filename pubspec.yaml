name: controle
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 SUPABASE (backend)
  supabase_flutter: ^2.12.0

  # 🔥 STATE MANAGEMENT
  flutter_riverpod: ^2.5.0

  # 🔥 GRÁFICOS (dashboard)
  fl_chart: ^0.66.0

  # 🔥 PDF (você já usa)
  pdf: ^3.12.0
  printing: ^5.14.3

  # 🔥 FORMATOS (datas, moeda)
  intl: ^0.18.0
  app_links: ^6.4.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true